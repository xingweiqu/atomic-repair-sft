# PREREG_naturalset — 真实世界乱 prompt 采样映射(C-12 B3;采样前冻结,2026-07-12)

> 目的:证明探针体系对应真病(construct validity 的外部锚)。零卡,采样+重标注。

## 1. 来源与配额(总 200,现成数据集,不自采)

| 来源 | n | 对应病 |
|---|---|---|
| CREPE(错误前提真实提问,主料) | 50 | W2 类(错误断言进题面) |
| GSM-IC(干扰注入) | 30 | W1 类近亲(错误/无关中间信息) |
| FalseQA / QA²(不可答假设问) | 30 | abstain 类 |
| sycophancy 评测(用户断言错答案) | 30 | W2/keep 类(服从压力) |
| NQ-Swap / ConflictQA(RAG 冲突) | 30 | K-冲突类 |
| RGB(RAG 噪声) | 30 | 噪声鲁棒类 |

采样规则:各源官方 split 随机抽(seed 42),去重,英文,单条 ≤2048 chars。

## 2. 标注 schema(冻结)

`{id, source, source_id, 原文, 错误信息类型, 映射探针(W1/W2/K/abstain/F/P/无), 映射理由一句}`
- 映射探针 = 我们探针体系里"这条真实失败对应哪台仪器";**允许"无"**(体系盲区,
  盲区率如实报——它是图 8 右的诚实列)。
- **双标注**:CC 全 200 + Xingwei 复核重叠 50,报一致率(Cohen κ);
  κ<0.6 则 schema 返工重标,不硬上。

## 3. 预测(冻结,弱形式)

- P-NS-1:≥70% 的真实条目可映射到既有探针(体系覆盖大半真病);
- P-NS-2:盲区条目集中在多轮/工具类(单轮探针体系的已知边界,如实入 limitation);
- P-NS-3:CREPE/sycophancy 主要落 W2、GSM-IC 落 W1(构念对齐性检验)。

## 4. 产物与差分纪律

图 8 右(来源×探针映射矩阵 + 盲区列);related work 每源一行差分
("他们测大象一条腿,我们的探针体系是整张病情表");
**GSM-IC↔W1 等重叠如实写,记邻居不记首创**(承 CL-4 双重记账先例)。
