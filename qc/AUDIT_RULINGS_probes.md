# 探针构念审计判决(Xingwei 亲审,2026-07-12)【存档】

判决:O ✅(附 hard 池连贯性过滤硬要求)/ C ✅(~92)/ W1 ✅(~88,点名表达式=保守偏差入披露)
/ W2 ⚠️ 条件过(16/100 负值 w,加合理性 validator:正值且 w/gold∈[0.2,5],重生成该批)
/ P ❌(100/100 think 残渣;修=strip+重跑;重审需原题-改写配对展示)
/ R ❌(残渣+构念漂移:频繁点名本题实体=本题方法摘要非通用规则;新口径=零本题实体名词+validator)
/ S ❌ 口径级损坏(最后一步表达式求值=gold、后步嵌前步结果——validator 只查字符串被表达式绕过;
  新口径=动词短语步骤、零算式零前步数值;新增表达式求值 validator,进 taxonomy)
/ F ⏸ 暂缓(审计样本缺双变体指令对,补 20 条再判;硬停发现独立成立)。

三裁决:S=(b) 修口径重生成,(a) 否决(构念损坏披露救不了);
F=双重记账(F_judge 测量角色保留 + "格式敏感"入册独立失败类;定位=体裁耦合计算在 pre-repair
先天存在,T4=放大先天耦合,§5 升级;CLAIMS 记"受控复现+新联结"不记首创,邻居=Let Me Speak
Freely 系;"表达式不计算"8 条单列子模式);
仪器教训=think 残渣与 pass@8 同族第二次 → **常设铁律:LLM 生成文本入探针/训练数据前必过
模板残渣 lint**(textlint.py)。

执行序:①hard 连贯性过滤+lint 上线+P strip 重跑;②S/R 新口径重生成→重审计(各再抽 100)
→重跑;W2 16 类重生成;F 补对照 20 条;③profile 表等七列全干净(O/C/W1/W2 推理保留)。
