# T4 判决 + batch-1 回收报告(2026-07-04)

> 输入:服务器 commit f5cf0aa(4 文件,行数 28/28/501/600 全对,commit 干净)。
> 本文是 R-5(第六类伪影命名)的裁决材料。判读表(LOOP1_RULINGS §2)没有预设本次出现的
> 混合格局,按纪律不硬塞行,如实呈报。

## 1. T4 素题双体裁对照(28 条 = repair-mode 下 floor 全错、pre-repair 全对的 matched 题)

| 模型 | 素题体裁产出 | 内容正确 |
|---|---|---|
| pre-repair | 28/28 素题体裁 | **28/28 = 100%** |
| scaffold_conv(30ep 收敛 floor) | 素题体裁 13 · **JSON 出血 11** · 哑火 4 | 素题体裁 **13/13 = 100%**;JSON 出血 **3/11 = 27%**;总分 16/28 = 57%(严格素题判分 46%) |

**核心事实:同一批题、同一份权重,算术对错跟着体裁走。**
- floor 守住素题体裁时:算术**零损伤**(13/13)。
- floor 滑进它被训练的 JSON 体裁时(给素题自创 `solve_decision/solve_policy/step_trace` schema):
  算术大面积错(3/11)——与 T2 在 repair-mode 里看到的 27/28 真算错**同源**。
- 出血率本身是训练强度的函数:重训后该体裁入侵素题输入(11 出血 + 4 哑火 = 15/28)。

## 2. 判读(对照预注册判读表)

- **H3 已出局**(T2 27/28 真算错、T3 步数 KS p=1.0,见 LOOP1_5_DISCRIMINATION)。
- **H1(权重损伤)出局**:H1 预测素题体裁下也算错;实测素题体裁 13/13 全对。
- **H2 成立,且机制比预注册表述更锐利**:不止"repair 模式下测不准",而是
  **(a) repair 体裁内的计算本身是劣化的**(体裁内算错,不是算对了表达不出);
  **(b) 高 epoch 训练让该体裁入侵素题输入**(出血+哑火)。
  能力=权重:完好。能力=可靠可调用的行为:体裁依赖。
- 预注册判读表的"坍塌→H1 强成立"行**不适用**:表设计时把"坍塌"默认为"算错",
  实测坍塌分解为"体裁内算术完好 + 体裁出血算错/哑火"。此偏离如实登记。

## 3. R-5 命名建议(待顾问裁决,论文措辞维持中性直至裁决)

- 建议命名:**mode-interference artifact(genre-coupled computation)**——计算正确性与
  生成体裁耦合;repair-SFT 训练的是体裁,体裁携带自己的(劣化的)计算通道。
- 论文可主张(若采纳):repair-mode 下的 ability 读数**系统性低估**权重内能力;
  任何"修复损坏/提升了能力"的主张必须给**素题体裁对照列**(= R-7 双体裁闸门的判据列)。
- 连带问题请一并裁决:judge 是否加第三档"schema-tolerant"(承认出血 JSON 里的
  final_answer)?它会把 T4 floor 从 46%→57%,把出血的一部分从"F/哑火"移到"体裁内容错"。
  倾向:**不加进账本主口径**(口径冻结),只作 T4/双体裁分析的辅助读数。

## 4. pass@8 calib500 —— 读数隔离(SUSPECT,勿用于定桶)

四桶(500 题,v1 脚本):[0-25%] 164 · (25-50%] 54 · (50-75%] 61 · (75-100%] 221;0/8 有 101 题。
**疑点:greedy 下 pre-repair ≈94%,采样却 20% 全灭(0/8)——与 T4/transfer 图景不符。**
头号嫌疑:vllm chat template 默认 Qwen3 **thinking 模式**,`<think>` 吃满 1024 token 预算 →
截断判 0 → [0-25%] 桶被系统性灌水。v1 脚本没存原始样本,无法本地验尸。
处置:`scripts/pass8_gsm.py` 升 **v2**(`/no_think` 软开关 + max_tokens 2048 + 首 20 题原样本
sidecar 留档),**calib500 需用 v2 重跑**(RUNBOOK batch-1b)。v1 数字隔离存档,不进任何设计决策。
——这正是 Artifact Taxonomy 的 F 类(判定/截断伪影)在测量仪器自身上的又一例,写进论文附录。

## 5. R-8 落地:v2.1 转细账

新地板(inject ckpt × v2.1 eval,600 行)入账,v2.1 七行从绝对分转四层分解(账本 637 行完成分解):
Δraw +44~+58,全部呈 **F 主导**(F_judge −42 / F_parse +93~+100,M≈0,D/A 不可测)——与
v2/v3 同族:知识地板不产可解析输出,增益无法与格式习得分离。v2.1 的方法排名
(actionized 57.7 > randomskill 54.8 > decision 53.8 > skillcot 47.2 > cot 44.0,Δraw)在
F 层内幸存,历史结论定性不变、定位改变(格式门内混合增益,不作能力主张)。

## 6. 等待

1. 顾问对 §3 命名 + schema-tolerant 第三档的裁决(R-5)。
2. 服务器跑 batch-1b(pass@8 v2 重跑 calib500,附 sidecar)→ 定桶边界 → Loop 2A 设计。
3. T5 功效版随 Loop 2A 难题桶落地。epoch-sweep(Loop 1.5 sweep 部分)仍待服务器。
