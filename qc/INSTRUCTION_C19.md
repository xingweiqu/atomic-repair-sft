# CC_INSTRUCTION_C19 — 明日汇报材料包(零算力,今晚交付)

> 目标:Xingwei 明天组会汇报。全部任务为出图/整表/拼装,不跑任何新实验。
> 按优先级排列,做不完的从底部砍。风格沿 FIGURE_STYLE.md(Okabe-Ito、
> 单 seed 空心、数字溯源脚注)。产出统一放 `report_pack/` 并给一份清单。

## P1(必交):P0c 两张主图

**图 A:旧"体裁效应"的方差归因**
数据:P0c 的 231 ckpt × 16 格重评结果。
形式:方差分解条形图——Operation / Evidence / Interface / Answerability 四轴
(含主要交互项)各自解释的方差占比。
要点标注:Operation 轴主导、Evidence ≈ 0。
Caption 一句:"旧 plain/repair 二分的效应,在 factorial 坐标下几乎全部由
'要求模型执行什么操作'承载。"
脚注:数据源路径 + n=231。

**图 B:弃答普适税(一页大字报)**
形式:极简——一个大数字 "230 / 231" 居中 + 一行说明
"训练臂损伤'信息不足应答不可答'能力的比例" + 底部小字:判定口径
(abstain 保持率相对 base 下降的阈值,写实际用的数)与数据源。
可选副图:全臂 abstain 保持率的分布直方图(base 竖线参照)。

## P2(必交):P1 硬停表美化版

硬停文档 §三 的七行表转为 slide 格式:
- 行序:base → cleanreplay → 纯 keep → 纯 override → D4 平衡@600 → D4 平衡@2000;
- 三列:keep / override / 弃答保持;
- 视觉强调两处:cleanreplay 的 keep .464(安慰剂独自砸掉 keep)、
  base 的 override .242(盲从证据);
- caption 注明:3-seed 臂标注、其余单臂如实。

## P3(尽力而为,赶不上不硬凑):V-2 联合分初读

若今晚能算完(预测文件均在):按同题 family 重算联合配对准确率
(对孪生保留 ∧ 错孪生改写)+ 盲从率列,出一张五行小表
(base / cleanreplay / 纯 override / 纯 keep / 平衡@2000)。
**只出数不出结论**;slide 标注 "preliminary, zero-compute re-read"。
判读预测先 commit 再算(V-2 指令里已列三条)。
赶不上 → 汇报里不提,不许用估算数顶替。

## P4(拼装):材料包清单

1. 旧组会 docx 的 §开场–§第2步 页面(缝/三幕/账本/山脊)标记页码供引用;
2. 归档论文中直接可投影的成品图清单:Fig1 画像 / Fig2 配药 / Fig3 剂量 /
   Fig7 验证矩阵 / Table 2 记分卡(各附一句"这页讲什么");
3. 新图 A/B + P1 表 + (若有)V-2 小表;
4. `report_pack/INDEX.md`:按汇报五段(转向声明→新仪器→P0c 重审→弃答税→
   P1 硬停侦探故事→下一步)标注每段用哪几页素材。

## 禁令

- 不跑新实验、不出新数字(V-2 除外且按预注册流程);
- 三向实验的预期结果一个字不出现在任何材料里;
- 所有图内数字可溯源,图 A/B 首次亮相前自查一遍口径
  (方差分解的方法名写进脚注:用的什么分解就写什么,不美化)。
