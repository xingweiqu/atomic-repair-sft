# PREREG P2a 剂量粗筛·两段式(H1 元规则检验;段1前冻结,2026-08-03)

**组件**:D0 replay/D1 format/D2 verify/D3 revise/D5 provenance/D6 abstain
(D4 条件配对已由 P1/P2-1 覆盖)。纯组件臂,剂量 {60,600,2000},单 seed 42,
epochs {2,4}(P2-1 教训:e8 过冲风险,粗筛不铺)。共 6×3×2=36 训。
**两段式(H1 的正确姿势)**:
- 段1 = 只跑 60 剂量档(12 训)+ 16 格评测;收数后 **CC 依 60 档边际响应
  冻结一份"600/2000 档组件排序预测"(逐格)commit**;
- 段2 = 跑 600/2000 档(24 训)+ 评测;对照预测打分。
**H1 判据**:段1预测的组件排序在 600 与 2000 档的 Spearman ρ ≥ 0.8(逐格中位)
→ "60 题 pilot 即可开处方"成立(手册元规则);ρ<0.5 → H1 证伪,处方必须全剂量实测;
中间带如实报。
评测 = p1_eval(16 格 + joint + insuf)+ 弃答过冲列;三闸门 factorial 版;
多机分片:臂单 [IDX::N] 按机器切,产物落 HDFS 共享,机器完成写 done 标记。
