# BIG_PICTURE — 项目北极星（2026-07-05，Xingwei 定调）

> 本文件是全项目的顶层框架，由 Xingwei 亲自定调。**所有 notes、图表、命名、以及解冻后的
> 论文写作，都必须能对号入座到本文的四段之一**；对不上号的产出先停下来问。
> 它高于历任裁决文件的叙事部分（账本叙事、体裁叙事均降为本框架的组成部分）；
> 口径/闸门/协议类裁决（C-1~C-7、R 系列）继续有效，不受影响。

---

## 0. 初心（为什么做这一切）

真人用 LLM 不像 benchmark：他会换个说法问、会把道听途说的错话夹在 prompt 里、会漏给
关键信息、prompt 从来不干净。**benchmark 分数测的是标准条件下的表现，用户体感是
乱糟糟真实条件下的表现——两者经常对不上。这条缝就是整个博士要打的东西。**

- Paper 1 的 9 格不是本体论，是**模拟真实使用的九种糟糕方式**（换问法/夹错话/抽线索…），
  测的是鲁棒性，定位缝的形状。
- Paper 2 问：**花钱补这条缝，买到的是什么？** 会不会 benchmark 涨了、体感反而崩了？
  （答案：会——体裁税就是实锤。）

任何时候迷失，回到这一段。

## 四段故事（论文骨架 = 组会骨架 = 实验的对号表）

### §1 定义错误体系——为什么要原子能力，对比现有 benchmark

主张：真实使用的失败要拆到原子粒度才可诊断、可行动；且诊断能**预测这个失败修不修得好**：
决策型失败（被错话带偏/该验证没验证）便宜可修；能力型失败（真不会算）数据修不了。
分类法不是背景板，是方法第一步：先诊断 → 再决定修不修、怎么修。

已有资产：Paper 1 全部（9 格探针、双读出、CoT 非均匀）。
账本五科目（F/K/M/D/A）在此定位为**诊断的度量衡**，不是论文主角。

### §2 怎么修——operator 化修复数据，三个域

主张：对着失败类型构造修复轨迹（Corrupt 注入 + actionized trace），在
synthetic（可学性前沿域）/ GSM8K(+hard) / multihop QA（2Wiki，预注册域）三个域上实施。
训练协议 = 脊点规则（parse≥0.95 ∧ bleed≤5% 的最小 epoch）——"对等"的单位是火候不是轮数。

已有资产：v3/v4 注入器、山脊曲线、canonical floor=e8、Tier 2 生成器（开发中）。

### §3 修得对不对——一张主图：data 少、performance 好、没 bad case

主张（三合一）：
- **省**：targeted 数百条即到位；random 三倍数据也不稳 →【E1 data-size sweep，进行中】
- **好**：resist 0.6→1.00@e2，跨题型泛化 →【已有 + E3 3-seed 误差棒】
- **不留后遗症**：脊点停车 → 零出血、素题无损；naive 多训 → 31% 出血、素题 92→49
  →【山脊曲线，已闭合】
评测可信度脚手架 = 账本协议（F/M 闸门、双体裁体检、泄漏审计、预注册）——它是防弹衣，
回答"凭什么信这张图"，不抢主角。

**两条诚实红线（写死，违者打回）**：
1. 修的是**决策层失败**，不是全部失败。GSM 变好的诚实表述：被植入错值带偏的部分可修
   （又快又省）；算术本身错的部分数据修不了（A 平线是我们自己的实测）→ 诊断→修可修的→
   其余接工具。**禁止宣称"我们能修好 GSM8K"。**
2. "对症"的粒度是**失败类型**，不是 operator。v4 实测任一 targeted operator 都泛化决策；
   operator 级对角线只在合成域成立且被泄漏污染过。对照 = targeted vs random，
   不退回 operator 对角线宣称。

### §4 为什么我们的方式好——activation 层机制

主张：targeted 数据高效，因为它在权重里一致地写**同一个方向**；random 在乱涂。
证据链：steering 单方向即可翻转 resist（且不带体裁税→ SFT 是捆绑销售，决策可单卖）；
cos(steering 方向, ΔW_targeted) ≫ cos(·, ΔW_random)。→【E5，进行中】
体裁机制（T4 双体裁、出血）在此段作为"SFT 捆绑包里除决策外还装了什么"的机制证据。

## 实验对号表（每个实验必须锁到某段的某句话）

| 实验 | 状态 | 锁哪句 |
|---|---|---|
| 账本 92 run（Loop 1） | ✅ | §1 诊断度量衡 + §3 防弹衣 |
| 硬停验尸 T1–T5 + V-1 | ✅ | §4 体裁机制（+§3 "没 bad case"的反例组） |
| 山脊 epoch-sweep + 素题体检 | ✅ | §2 脊点协议 + §3 "不留后遗症" |
| pass@8 合并池（E2） | 🔄 | §3 剂量-响应的分桶地基 |
| data-size sweep（E1） | 🔄 | §3 主图的 x 轴（"省"） |
| 3-seed（E3） | 🔄 | §3 误差棒 |
| 2Wiki 筛选（E4） | 🔄 | §2 第三域 + §1 诊断跨域（K 缺口 vs A 缺口的 Aug 探针分离） |
| steering + 对齐（E5） | 🔄 | §4 全部 |
| Tier 2 前沿（Loop 2B） | ⏳ | §1 "能力型失败修不了"的边界线（多简单才修得了） |
| Loop 2A 剂量-响应 | ⏳ | §3 "缺口越大收益越大"的曲线 |
| R-17 重记账 | 🔄 | §3 全部数字的基线正确性 |

新实验提案的准入检验：**说不出锁哪句 → 不跑。**

## 措辞与纪律（沿用）

- C-7：禁 "only possible sources"；账本主张 = "decomposed without remainder into"。
- 全文用 pre-repair model，不用 base/pretrained 指代修复前 ckpt（D-6 待最终闭合）。
- 用户语言优先：D = 抗误导鲁棒性；体裁税 = "benchmark 涨、体感崩"；脊点 = 在体感最优处停车。
- 硬停 = 与当期预注册矛盾；论文写作冻结中，notes 不做跨实验综合。


---

## 修正案 A1(R-22,2026-07-06,顾问裁决;终版等 E1b)

- §3 "省"的现役证据 = **单 operator 配方**(660 条、3-seed 99/100/100、resist 持久至 e30);
  主图配方 E1b 后终裁。
- 红线 2 精化:对照的操作变量 = **"动作一致的修复数据 vs 动作稀释/无关数据"**;
  禁写"混合修复数据无效"——精确表述:"含 keep 的混合无效,operator-only 混合待 E1b 判"。
